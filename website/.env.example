# ==============================================================================
# MEGACLICK WEBSITE — FRONTEND ENVIRONMENT
# ==============================================================================

# --- API Configuration ---
# URL and port where the MegaClick backend is running. Contact-form submissions
# are POSTed here and land on the dashboard Leads board.
# Leave blank in production if the site and API share an origin.
# Leave blank in dev — vite.config.js proxies /api to the backend.
VITE_API_URL=
